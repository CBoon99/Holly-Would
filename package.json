{
  "name": "holly-would",
  "version": "0.1.0",
  "private": true,
  "description": "Holly Would — series · technical · hilarious. Hollywood acting practice.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "verify": "npm run verify --prefix web",
    "db:seed": "npm run db:seed --prefix web",
    "db:seed:hollywood": "npm run db:seed:hollywood --prefix web",
    "automate": "npm run automate --prefix web",
    "automate:hollywood": "npm run automate:hollywood --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
